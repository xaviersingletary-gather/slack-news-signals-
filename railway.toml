# Railway — DEFAULT config. Applies to the signal-ping-listener web service
# (no config path set = this file). The worker uses railway.worker.toml.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 listener.py"
restartPolicyType = "ON_FAILURE"
