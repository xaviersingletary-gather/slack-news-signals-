# copy values in yourself — never commit this file (see .gitignore)
# token lives in the SOS repo .env today:
#   grep SLACK_BOT_TOKEN ~/"Hermes - Signals Watcher"/.env >> ~/signal-ping/.env
SLACK_BOT_TOKEN=
# only needed when llm_filter: true in config.yaml
OPENROUTER_API_KEY=
